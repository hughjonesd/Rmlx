% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/math.R
\name{mlx_isclose}
\alias{mlx_isclose}
\title{Element-wise approximate equality}
\usage{
mlx_isclose(
  a,
  b,
  rtol = 1e-05,
  atol = 1e-08,
  equal_nan = FALSE,
  device = mlx_default_device()
)
}
\arguments{
\item{a, b}{MLX arrays or objects coercible to MLX arrays}

\item{rtol}{Relative tolerance (default: 1e-5)}

\item{atol}{Absolute tolerance (default: 1e-8)}

\item{equal_nan}{If \code{TRUE}, NaN values are considered equal (default: \code{FALSE})}

\item{device}{Execution target: supply \code{"gpu"}, \code{"cpu"}, or an
\code{mlx_stream} created via \code{\link[=mlx_new_stream]{mlx_new_stream()}}. Default: \code{mlx_default_device()}.}
}
\value{
An mlx array of booleans with element-wise comparison results
}
\description{
Returns a boolean array indicating which elements of two arrays are close
within specified tolerances.
}
\details{
Two values are considered close if:
\code{abs(a - b) <= (atol + rtol * abs(b))}

Infinite values with matching signs are considered equal.
Supports NumPy-style broadcasting.
}
\examples{
a <- as_mlx(c(1.0, 2.0, 3.0))
b <- as_mlx(c(1.0 + 1e-6, 2.0 + 1e-6, 3.0 + 1e-3))
mlx_isclose(a, b)  # First two TRUE, last FALSE
}
\seealso{
\code{\link[=mlx_allclose]{mlx_allclose()}}, \code{\link[=all.equal.mlx]{all.equal.mlx()}},
\url{https://ml-explore.github.io/mlx/build/html/python/_autosummary/mlx.core.isclose.html}
}
