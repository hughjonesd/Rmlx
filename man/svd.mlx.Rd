% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/linalg_adv.R
\name{svd.mlx}
\alias{svd.mlx}
\title{Singular value decomposition for mlx arrays}
\usage{
\method{svd}{mlx}(x, nu = min(n, p), nv = min(n, p), ..., device = NULL)
}
\arguments{
\item{x}{An mlx matrix (2-dimensional array).}

\item{nu}{Number of left singular vectors to return (0 or \code{min(dim(x))}).}

\item{nv}{Number of right singular vectors to return (0 or \code{min(dim(x))}).}

\item{...}{Additional arguments; ignored.}

\item{device}{Execution target for APIs that expose a one-off device or
stream override. Supply \code{"gpu"}, \code{"cpu"}, or an \code{mlx_stream} created via
\code{\link[=mlx_new_stream]{mlx_new_stream()}}. Ordinary array operations use the current
\code{\link[=mlx_device]{mlx_device()}} instead.}
}
\value{
A list with components \code{d}, \code{u}, and \code{v}.
}
\description{
Note that mlx's svd returns "full" SVD, with U and V' both square matrices.
This is different from R's implementation.
}
\details{
As of MLX 0.31.1, this operation only runs on CPU. Run it inside
\code{\link[=with_device]{with_device()}} or \code{\link[=local_device]{local_device()}}, or pass \code{device = "cpu"} when the
function exposes that argument.
}
\examples{
x <- mlx_matrix(c(1, 0, 0, 2), 2, 2)
svd(x, device = "cpu")
}
\seealso{
\href{https://ml-explore.github.io/mlx/build/html/python/linalg.html#mlx.linalg.svd}{mlx.linalg.svd}
}
