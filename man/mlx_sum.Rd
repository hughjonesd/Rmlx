% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/stats.R
\name{mlx_sum}
\alias{mlx_sum}
\alias{mlx_prod}
\alias{mlx_all}
\alias{mlx_any}
\alias{mlx_mean}
\alias{mlx_var}
\alias{mlx_std}
\alias{mlx_sd}
\title{Reduce mlx arrays}
\usage{
mlx_sum(x, axes = NULL, drop = TRUE)

mlx_prod(x, axes = NULL, drop = TRUE)

mlx_all(x, axes = NULL, drop = TRUE)

mlx_any(x, axes = NULL, drop = TRUE)

mlx_mean(x, axes = NULL, drop = TRUE)

mlx_var(x, axes = NULL, drop = TRUE, ddof = 0L)

mlx_std(x, axes = NULL, drop = TRUE, ddof = 0L)

mlx_sd(x, axes = NULL, drop = TRUE)
}
\arguments{
\item{x}{An mlx array, or an R array/matrix/vector that will be converted via \code{\link[=as_mlx]{as_mlx()}}.}

\item{axes}{Integer vector of axes (1-indexed). Supply positive integers
between 1 and the array rank. Many helpers interpret \code{NULL} to mean "all
axes"—see the function details for specifics.}

\item{drop}{If \code{TRUE} (default), drop dimensions of length 1. If \code{FALSE},
retain all dimensions. Equivalent to \code{keepdims = TRUE} in underlying
mlx functions.}

\item{ddof}{Non-negative integer delta degrees of freedom for variance or
standard deviation reductions.}
}
\value{
An mlx array containing the reduction result.
}
\description{
These helpers mirror NumPy-style reductions, optionally collapsing one or
more axes. Use \code{drop = FALSE} to retain reduced axes with length one
(akin to setting \code{drop = FALSE} in base R).
}
\details{
\code{mlx_all()} and \code{mlx_any()} return mlx boolean scalars, while the
base R reducers \code{\link[=all]{all()}} and \code{\link[=any]{any()}} applied to mlx inputs return plain
logical scalars.

The \code{axes} argument is the inverse of \code{MARGIN} in base R
\code{\link[=apply]{apply()}}. \code{axes} gives the axes which will be reduced; \code{MARGIN}
gives the axes which an operation will be applied over. See the example.

\code{mlx_sd()} is a convenience wrapper that matches the default behaviour
of \code{\link[stats:sd]{stats::sd()}}, computing a sample standard deviation with \code{ddof = 1}.
}
\examples{
x <- mlx_matrix(1:4, 2, 2)
mlx_sum(x)
mlx_sum(x, axes = 1)
mlx_prod(x, axes = 2, drop = FALSE)
mlx_all(x > 0)
mlx_any(x > 3)
mlx_mean(x, axes = 1)
mlx_var(x, axes = 2)
mlx_std(x)
mlx_sd(x)
# for comparison:
stats::sd(as.matrix(x))

a <- array(1:6, dim = 1:3)
ax <- as_mlx(a)
# These are equivalent:
apply(a, 1:2, sum) # leaves dimensions 1-2 intact, sums over dimension 3
mlx_sum(a, 3)      # the same
}
\seealso{
\href{https://ml-explore.github.io/mlx/build/html/python/array.html#mlx.core.sum}{mlx.core.sum},
\href{https://ml-explore.github.io/mlx/build/html/python/array.html#mlx.core.prod}{mlx.core.prod},
\href{https://ml-explore.github.io/mlx/build/html/python/array.html#mlx.core.all}{mlx.core.all},
\href{https://ml-explore.github.io/mlx/build/html/python/array.html#mlx.core.any}{mlx.core.any},
\href{https://ml-explore.github.io/mlx/build/html/python/array.html#mlx.core.mean}{mlx.core.mean},
\href{https://ml-explore.github.io/mlx/build/html/python/array.html#mlx.core.var}{mlx.core.var},
\href{https://ml-explore.github.io/mlx/build/html/python/array.html#mlx.core.std}{mlx.core.std}
}
