% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/indexing.R
\name{mlx_slice_update}
\alias{mlx_slice_update}
\title{Update a slice of an mlx array}
\usage{
mlx_slice_update(x, value, start, stop, strides = NULL)
}
\arguments{
\item{x}{An mlx array.}

\item{value}{Replacement \code{mlx} (or coercible) array. Must broadcast to the
slice determined by \code{start}, \code{stop}, and \code{strides}.}

\item{start}{Integer vector (1-indexed) giving the inclusive starting index
for each axis.}

\item{stop}{Integer vector (1-indexed) giving the inclusive stopping index
for each axis.}

\item{strides}{Optional integer vector of strides (defaults to ones).}
}
\value{
An \code{mlx} array with the specified slice replaced.
}
\description{
Wrapper around \href{https://ml-explore.github.io/mlx/build/html/python/array.html#mlx.core.slice_update}{\code{mlx.core.slice_update()}}
that replaces a contiguous strided region with \code{value}.
}
\examples{
x <- mlx_matrix(1:9, 3, 3)
replacement <- mlx_matrix(100:103, nrow = 2)
updated <- mlx_slice_update(x, replacement, start = c(1L, 2L), stop = c(2L, 3L))
updated
}
