% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/stats.R
\name{mlx_cumsum}
\alias{mlx_cumsum}
\alias{mlx_cumprod}
\title{Cumulative sum and product}
\usage{
mlx_cumsum(x, axis = NULL, reverse = FALSE, inclusive = TRUE)

mlx_cumprod(x, axis = NULL, reverse = FALSE, inclusive = TRUE)
}
\arguments{
\item{x}{An mlx array, or an R array/matrix/vector that will be converted via \code{\link[=as_mlx]{as_mlx()}}.}

\item{axis}{Single axis (1-indexed). Supply a positive integer between 1 and
the array rank. Use \code{NULL} when the helper interprets it as "all axes" (see
individual docs).}

\item{reverse}{If \code{TRUE}, compute in reverse order.}

\item{inclusive}{If \code{TRUE} (default), include the current element in the cumulative operation.
If \code{FALSE}, the cumulative operation is exclusive (starts from identity element).}
}
\value{
An mlx array with cumulative sums or products.
}
\description{
Compute cumulative sums or products along an axis.
}
\details{
When \code{axis} is \code{NULL} (default), the array is flattened before
computing the cumulative result.
}
\examples{
x <- as_mlx(1:5)
mlx_cumsum(x)  # [1, 3, 6, 10, 15]

mat <- as_mlx(matrix(1:12, 3, 4))
mlx_cumsum(mat, axis = 1)  # cumsum down rows
}
\seealso{
\code{\link[=cumsum]{cumsum()}}, \code{\link[=cumprod]{cumprod()}},
\href{https://ml-explore.github.io/mlx/build/html/python/array.html#mlx.core.cumsum}{mlx.core.cumsum},
\href{https://ml-explore.github.io/mlx/build/html/python/array.html#mlx.core.cumprod}{mlx.core.cumprod}
}
