% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/stats.R
\name{mlx_dlogis}
\alias{mlx_dlogis}
\alias{mlx_plogis}
\alias{mlx_qlogis}
\title{Logistic distribution functions}
\usage{
mlx_dlogis(
  x,
  location = 0,
  scale = 1,
  log = FALSE,
  device = mlx_default_device()
)

mlx_plogis(x, location = 0, scale = 1, device = mlx_default_device())

mlx_qlogis(p, location = 0, scale = 1, device = mlx_default_device())
}
\arguments{
\item{x}{Vector of quantiles (mlx array or coercible to mlx)}

\item{location, scale}{Location and scale parameters (default: 0, 1)}

\item{log}{If \code{TRUE}, return log density for \code{mlx_dlogis} (default: \code{FALSE})}

\item{device}{Execution target: supply \code{"gpu"}, \code{"cpu"}, or an
\code{mlx_stream} created via \code{\link[=mlx_new_stream]{mlx_new_stream()}}. Default: \code{mlx_default_device()}.}

\item{p}{Vector of probabilities (mlx array or coercible to mlx)}
}
\value{
An mlx array with the computed values
}
\description{
Compute density (\code{mlx_dlogis}), cumulative distribution (\code{mlx_plogis}),
and quantile (\code{mlx_qlogis}) functions for the logistic distribution using MLX.
}
\examples{
x <- as_mlx(seq(-3, 3, by = 0.5))
as.matrix(mlx_dlogis(x))
as.matrix(mlx_plogis(x))

p <- as_mlx(c(0.25, 0.5, 0.75))
as.matrix(mlx_qlogis(p))
}
