% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fast.R
\name{mlx_metal_kernel}
\alias{mlx_metal_kernel}
\title{Create a JIT-compiled custom Metal kernel}
\usage{
mlx_metal_kernel(
  name,
  input_names,
  output_names,
  source,
  header = "",
  ensure_row_contiguous = TRUE,
  atomic_outputs = FALSE
)
}
\arguments{
\item{name}{Kernel name used in generated Metal code.}

\item{input_names}{Character vector naming the kernel inputs.}

\item{output_names}{Character vector naming the kernel outputs.}

\item{source}{Metal source for the kernel body. MLX generates the function
signature automatically.}

\item{header}{Optional Metal source prepended before the generated function.}

\item{ensure_row_contiguous}{Logical. Should MLX make inputs row-contiguous
before launching the kernel?}

\item{atomic_outputs}{Logical. Should output buffers use Metal atomic types?}
}
\value{
A function that executes the compiled kernel and returns one \code{mlx}
array for a single output or a named list of \code{mlx} arrays otherwise.
}
\description{
Wraps MLX's Metal kernel API so R code can define custom GPU kernels while
keeping inputs and outputs as \code{mlx} arrays.
}
\examples{
\dontrun{
add_one <- mlx_metal_kernel(
  name = "add_one",
  input_names = "inp",
  output_names = "out",
  source = "
    uint elem = thread_position_in_grid.x;
    out[elem] = inp[elem] + (T)1;
  "
)

x <- mlx_cast(as_mlx(1:8), "float32")
y <- add_one(
  inputs = list(x),
  output_shapes = list(c(length(x))),
  output_dtypes = "float32",
  grid = c(length(x), 1L, 1L),
  threadgroup = c(length(x), 1L, 1L),
  template = list(T = "float32")
)
}
}
\seealso{
\code{\link[=mlx_compile]{mlx_compile()}}

\href{https://ml-explore.github.io/mlx/build/html/python/_autosummary/mlx.core.fast.metal_kernel.html}{mlx.core.fast.metal_kernel}

\href{https://ml-explore.github.io/mlx/build/html/dev/custom_metal_kernels.html}{Custom Metal Kernels}
}
