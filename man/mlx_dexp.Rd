% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/stats.R
\name{mlx_dexp}
\alias{mlx_dexp}
\alias{mlx_pexp}
\alias{mlx_qexp}
\title{Exponential distribution functions}
\usage{
mlx_dexp(x, rate = 1, log = FALSE, device = mlx_default_device())

mlx_pexp(x, rate = 1, device = mlx_default_device())

mlx_qexp(p, rate = 1, device = mlx_default_device())
}
\arguments{
\item{x}{Vector of quantiles (mlx array or coercible to mlx)}

\item{rate}{Rate parameter (default: 1)}

\item{log}{If \code{TRUE}, return log density for \code{mlx_dexp} (default: \code{FALSE})}

\item{device}{Execution target: supply \code{"gpu"}, \code{"cpu"}, or an
\code{mlx_stream} created via \code{\link[=mlx_new_stream]{mlx_new_stream()}}. Defaults to the current
\code{\link[=mlx_default_device]{mlx_default_device()}} unless noted otherwise (helpers that act on an existing
array typically reuse that array's device or stream).}

\item{p}{Vector of probabilities (mlx array or coercible to mlx)}
}
\value{
An mlx array with the computed values.
}
\description{
Compute density (\code{mlx_dexp}), cumulative distribution (\code{mlx_pexp}),
and quantile (\code{mlx_qexp}) functions for the exponential distribution using MLX.
}
\examples{
x <- as_mlx(seq(0, 5, by = 0.5))
mlx_dexp(x)
mlx_pexp(x)

p <- as_mlx(c(0.25, 0.5, 0.75))
mlx_qexp(p)
}
