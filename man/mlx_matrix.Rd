% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/create.R
\name{mlx_matrix}
\alias{mlx_matrix}
\title{Construct MLX matrices efficiently}
\usage{
mlx_matrix(
  data,
  nrow = NULL,
  ncol = NULL,
  byrow = FALSE,
  dtype = NULL,
  device = mlx_default_device()
)
}
\arguments{
\item{data}{Numeric, logical, or complex vector. \code{data} is recycled to
match dimensions according to R rules (but with an error if it doesn't
tile into the dimensions exactly).}

\item{nrow, ncol}{Matrix dimensions (positive integers).}

\item{byrow}{Logical; if \code{TRUE}, fill by rows (same semantics as \code{\link[base:matrix]{base::matrix()}}).}

\item{dtype}{Data type string. Supported types include:
\itemize{
\item Floating point: \code{"float32"}, \code{"float64"}
\item Integer: \code{"int8"}, \code{"int16"}, \code{"int32"}, \code{"int64"}, \code{"uint8"}, \code{"uint16"}, \code{"uint32"}, \code{"uint64"}
\item Other: \code{"bool"}, \code{"complex64"}
}

\code{float64} arrays are CPU-only. Use \code{device = "cpu"} when creating or
casting to \code{float64}, and cast back to \code{float32} before using the GPU.
Not all functions support all types. See individual function documentation.}

\item{device}{Execution target: supply \code{"gpu"}, \code{"cpu"}, or an
\code{mlx_stream} created via \code{\link[=mlx_new_stream]{mlx_new_stream()}}. By default, many
functions use the \code{\link[=mlx_device]{mlx_device()}} of their first argument.}
}
\value{
An \code{mlx} matrix with \code{dim = c(nrow, ncol)}.
}
\description{
\code{mlx_matrix()} wraps \code{\link[=mlx_array]{mlx_array()}} for the common 2-D case. It accepts the same
style arguments as \code{\link[base:matrix]{base::matrix()}} but without recycling, so mistakes surface early.
Supply \code{nrow} or \code{ncol} (the other may be inferred from \code{length(data)}).
}
\examples{
mlx_matrix(1:6, nrow = 2, ncol = 3, byrow = TRUE)
}
