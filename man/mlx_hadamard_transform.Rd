% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/transform.R
\name{mlx_hadamard_transform}
\alias{mlx_hadamard_transform}
\title{Hadamard transform for MLX arrays}
\usage{
mlx_hadamard_transform(x, scale = NULL)
}
\arguments{
\item{x}{An mlx array, or an R array/matrix/vector that will be converted via \code{\link[=as_mlx]{as_mlx()}}.}

\item{scale}{Optional numeric scalar applied to the result. MLX defaults to
\code{1 / sqrt(n)} where \code{n} is the size of the transformed axis; set \code{scale}
to override the factor (for example, \code{scale = 1} yields the unnormalised
Hadamard transform).}
}
\value{
An \code{mlx} array containing the Hadamard-transformed values.
}
\description{
Multiplies the last dimension of \code{x} by the Sylvester-Hadamard matrix of the
corresponding size. The transform expects the length of the last axis to be a
power of two.
}
\examples{
x <- as_mlx(c(1, -1))
as.vector(mlx_hadamard_transform(x))
as.vector(mlx_hadamard_transform(x, scale = 1))
}
\seealso{
\url{https://ml-explore.github.io/mlx/build/html/python/array.html#mlx.core.hadamard_transform}
}
