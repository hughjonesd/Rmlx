% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/concat.R
\name{abind}
\alias{abind}
\title{Bind mlx arrays along an axis}
\usage{
abind(..., along = 1L)
}
\arguments{
\item{...}{One or more mlx arrays (or a single list of arrays) to combine.}

\item{along}{Positive integer giving the existing axis (1-indexed) along which
to bind the inputs.}
}
\value{
An mlx array formed by concatenating the inputs along \code{along}.
}
\description{
Bind mlx arrays along an axis
}
\details{
This is an MLX-backed alternative to \code{\link[abind:abind]{abind::abind()}}. All inputs must share
the same shape on non-bound axes. The \code{along} axis must already exist; to
create a new axis use \code{\link[=mlx_stack]{mlx_stack()}}.
}
\examples{
x <- as_mlx(array(1:12, c(2, 3, 2)))
y <- as_mlx(array(13:24, c(2, 3, 2)))
z <- abind(x, y, along = 3)
dim(z)
}
\seealso{
\url{https://ml-explore.github.io/mlx/build/html/python/array.html#mlx.core.concatenate}
}
