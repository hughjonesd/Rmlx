% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/shape.R
\name{mlx_broadcast_arrays}
\alias{mlx_broadcast_arrays}
\title{Broadcast multiple arrays to a shared shape}
\usage{
mlx_broadcast_arrays(..., device = NULL)
}
\arguments{
\item{...}{One or more tensors (or a single list) convertible via \code{\link[=as_mlx]{as_mlx()}}.}

\item{device}{Optional device override (\code{"gpu"} or \code{"cpu"}). Defaults to the common device of the inputs.}
}
\value{
A list of broadcast mlx arrays.
}
\description{
\code{mlx_broadcast_arrays()} mirrors \href{https://ml-explore.github.io/mlx/build/html/python/array.html#mlx.core.broadcast_arrays}{\code{mlx.core.broadcast_arrays()}},
returning a list of inputs expanded to a common shape.
}
\examples{
a <- as_mlx(matrix(1:3, nrow = 1))
b <- as_mlx(matrix(1:3, ncol = 1))
outs <- mlx_broadcast_arrays(a, b)
lapply(outs, dim)
}
\seealso{
\url{https://github.com/ml-explore/mlx/blob/main/python/mlx/core/array.py}
}
