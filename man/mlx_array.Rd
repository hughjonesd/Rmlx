% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/create.R
\name{mlx_array}
\alias{mlx_array}
\title{Construct an MLX array from R data}
\usage{
mlx_array(data, dim, dtype = NULL, device = mlx_default_device())
}
\arguments{
\item{data}{Numeric, logical, or complex vector supplying the payload.
Any dimension attributes are ignored; pass \code{dim} explicitly.}

\item{dim}{Integer vector of array dimensions (product must equal \code{length(data)}).}

\item{dtype}{Optional MLX dtype. Defaults to \code{"float32"} for numeric input,
\code{"bool"} for logical, and \code{"complex64"} for complex.}

\item{device}{Execution target: provide \code{"gpu"}, \code{"cpu"}, or an
\code{mlx_stream} created via \code{\link[=mlx_new_stream]{mlx_new_stream()}}. Defaults to the current
\code{\link[=mlx_default_device]{mlx_default_device()}}.}
}
\value{
An \code{mlx} array with the requested shape.
}
\description{
\code{mlx_array()} is a low-level constructor that skips \code{as_mlx()}'s type inference
and dimension guessing. Supply the raw payload vector plus an explicit shape
and it pipes the data straight into MLX.
}
\examples{
payload <- runif(6)
arr <- mlx_array(payload, dim = c(2, 3))
as.matrix(arr)
}
