% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/class.R
\name{as_mlx}
\alias{as_mlx}
\title{Create MLX array from R object}
\usage{
as_mlx(
  x,
  dtype = c("float32", "float64", "bool", "complex64", "int8", "int16", "int32", "int64",
    "uint8", "uint16", "uint32", "uint64"),
  device = mlx_default_device()
)
}
\arguments{
\item{x}{Numeric, logical, or complex vector, matrix, or array to convert}

\item{dtype}{Data type for the MLX array. One of:
\itemize{
\item Floating point: \code{"float32"}, \code{"float64"}
\item Integer signed: \code{"int8"}, \code{"int16"}, \code{"int32"}, \code{"int64"}
\item Integer unsigned: \code{"uint8"}, \code{"uint16"}, \code{"uint32"}, \code{"uint64"}
\item Other: \code{"bool"}, \code{"complex64"}
}

If not specified, defaults to \code{"float32"} for numeric, \code{"bool"} for logical,
and \code{"complex64"} for complex inputs.}

\item{device}{Execution target: supply \code{"gpu"}, \code{"cpu"}, or an
\code{mlx_stream} created via \code{\link[=mlx_new_stream]{mlx_new_stream()}}. By default, many
functions use the \code{\link[=mlx_device]{mlx_device()}} of their first argument.}
}
\value{
An object of class \code{mlx}
\subsection{Integer types require explicit dtype}{

R integer vectors (like \code{1:10}) convert to \code{float32} by default.
To create integer MLX arrays, you must explicitly specify \code{dtype}:

\if{html}{\out{<div class="sourceCode r">}}\preformatted{x <- as_mlx(1:10, dtype = "int32")  # Creates int32 array
x <- as_mlx(1:10)                    # Creates float32 array
}\if{html}{\out{</div>}}
}

\subsection{Type precision}{
\itemize{
\item \code{float64} is supported on CPU only; use \code{device = "cpu"} explicitly.
Cast GPU arrays to \code{float64} with \code{mlx_cast(x, dtype = "float64", device = "cpu")}, and cast back to \code{float32} before returning to GPU.
\item Integer arithmetic may promote types (e.g., int32 + int32 might → int64)
\item Mixed integer/float operations promote to float
}
}

\subsection{Missing values}{

MLX does not have an \code{NA} sentinel. When you pass numeric \code{NA} values from R,
they are stored as \code{NaN} inside MLX and returned to R as \code{NaN}.
Use \code{\link[=is.nan]{is.nan()}} on MLX arrays if you need to detect them. \code{\link[=is.na]{is.na()}} on mlx
objects calls \code{\link[=is.nan]{is.nan()}}.
}

\subsection{Scalars}{

MLX allows scalar values, with a zero-length dimension (\code{integer(0)}). These
are not usually what R users want. \code{as_mlx()} never returns a scalar; call
\verb{[mlx_reshape(x, integer(0))][mlx_reshape()]} to create one explicitly, or
use \verb{[mlx_array(..., allow_scalar = TRUE)][mlx_array()]}.
}
}
\description{
Create MLX array from R object
}
\examples{
# Default float32 for numeric
x <- as_mlx(c(1.5, 2.5, 3.5))
mlx_dtype(x)  # "float32"

# R integers also default to float32
x <- as_mlx(1:10)
mlx_dtype(x)  # "float32"

# Explicit integer types
x_int <- as_mlx(1:10, dtype = "int32")
mlx_dtype(x_int)  # "int32"

# Unsigned integers
x_uint <- as_mlx(c(0, 128, 255), dtype = "uint8")

# Logical → bool
mask <- as_mlx(c(TRUE, FALSE, TRUE))
mlx_dtype(mask)  # "bool"
}
\seealso{
\href{https://ml-explore.github.io/mlx/build/html/python/array.html#mlx.core.array}{mlx.core.array}

\link{mlx-methods}
}
