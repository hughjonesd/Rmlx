% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/compile.R
\name{mlx_disable_compile}
\alias{mlx_disable_compile}
\alias{mlx_enable_compile}
\title{Control Global Compilation Behavior}
\usage{
mlx_disable_compile()

mlx_enable_compile()
}
\value{
Invisibly returns \code{NULL}.
}
\description{
\itemize{
\item \code{mlx_disable_compile()} prevents all compilation globally. Compiled
functions will execute without optimization.
\item \code{mlx_enable_compile()} enables compilation (overrides the
\code{MLX_DISABLE_COMPILE} environment variable).
}
}
\details{
These functions control whether MLX compilation is enabled globally.

These are useful for debugging (to check if compilation is causing issues)
or benchmarking (to measure compilation overhead vs speedup).

You can also disable compilation by setting the \code{MLX_DISABLE_COMPILE}
environment variable before loading the package.
}
\examples{
\dontrun{
# Disable compilation for debugging
mlx_disable_compile()
result <- compiled_fn(x)  # Runs without optimization

# Re-enable compilation
mlx_enable_compile()
result <- compiled_fn(x)  # Runs with optimization
}

}
