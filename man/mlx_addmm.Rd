% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ops.R
\name{mlx_addmm}
\alias{mlx_addmm}
\title{Fused matrix multiply and add for MLX arrays}
\usage{
mlx_addmm(input, mat1, mat2, alpha = 1, beta = 1)
}
\arguments{
\item{input}{Matrix-like object providing the additive term.}

\item{mat1}{Left matrix operand.}

\item{mat2}{Right matrix operand.}

\item{alpha, beta}{Numeric scalars controlling the fused linear combination.}
}
\value{
An \code{mlx} matrix with the same shape as \code{input}.
}
\description{
Computes \code{beta * input + alpha * (mat1 \%*\% mat2)} in a single MLX kernel.
All operands are promoted to a common dtype/device prior to evaluation.
}
\examples{
\dontrun{
input <- as_mlx(diag(3))
mat1 <- as_mlx(matrix(rnorm(9), 3, 3))
mat2 <- as_mlx(matrix(rnorm(9), 3, 3))
mlx_addmm(input, mat1, mat2, alpha = 0.5, beta = 2)
}
}
\seealso{
\url{https://ml-explore.github.io/mlx/build/html/python/array.html#mlx.core.addmm}
}
