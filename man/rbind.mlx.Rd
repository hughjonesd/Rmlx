% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/concat.R
\name{rbind.mlx}
\alias{rbind.mlx}
\title{Row-bind mlx arrays}
\usage{
\method{rbind}{mlx}(..., deparse.level = 1)
}
\arguments{
\item{...}{Objects to bind. mlx arrays are kept in MLX; other inputs are
coerced via \code{as_mlx()}.}

\item{deparse.level}{Compatibility argument accepted for S3 dispatch; ignored.}
}
\value{
An mlx array stacked along the first axis.
}
\description{
Row-bind mlx arrays
}
\details{
Unlike base R's \code{rbind()}, this function supports arrays with more
than 2 dimensions and preserves all dimensions except the first (which is
summed across inputs). Base R's \code{rbind()} flattens higher-dimensional arrays
to matrices before binding.
}
\examples{
x <- mlx_matrix(1:4, 2, 2)
y <- mlx_matrix(5:8, 2, 2)
rbind(x, y)
}
\seealso{
\href{https://ml-explore.github.io/mlx/build/html/python/array.html#mlx.core.concatenate}{mlx.core.concatenate}
}
