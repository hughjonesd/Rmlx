% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/linalg_adv.R
\name{chol2inv}
\alias{chol2inv}
\alias{chol2inv.default}
\alias{chol2inv.mlx}
\title{Inverse from Cholesky decomposition}
\usage{
chol2inv(x, size = NCOL(x), ...)

\method{chol2inv}{default}(x, size = NCOL(x), ...)

\method{chol2inv}{mlx}(x, size = NCOL(x), ..., device = NULL)
}
\arguments{
\item{x}{An mlx matrix (2-dimensional array).}

\item{size}{Ignored; included for compatibility with base R.}

\item{...}{Additional arguments; ignored.}

\item{device}{Execution target for APIs that expose a one-off device or
stream override. Supply \code{"gpu"}, \code{"cpu"}, or an \code{mlx_stream} created via
\code{\link[=mlx_new_stream]{mlx_new_stream()}}. Ordinary array operations use the current
\code{\link[=mlx_device]{mlx_device()}} instead.}
}
\value{
The inverse of the original matrix (before Cholesky decomposition).
}
\description{
Compute the inverse of a symmetric, positive definite matrix from its
Cholesky decomposition. The input \code{x} should be an upper triangular matrix
from \code{chol()}.
}
\details{
As of MLX 0.31.1, this operation only runs on CPU. Run it inside
\code{\link[=with_device]{with_device()}} or \code{\link[=local_device]{local_device()}}, or pass \code{device = "cpu"}.
}
\examples{
A <- mlx_matrix(c(4, 1, 1, 3), 2, 2)
U <- chol(A, device = "cpu")
A_inv <- chol2inv(U, device = "cpu")
# Verify: A \%*\% A_inv should be identity
A \%*\% A_inv
}
\seealso{
\code{\link[=chol]{chol()}}, \code{\link[=solve]{solve()}}, \code{\link[=mlx_cholesky_inv]{mlx_cholesky_inv()}}
}
