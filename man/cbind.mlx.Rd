% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/concat.R
\name{cbind.mlx}
\alias{cbind.mlx}
\title{Column-bind mlx arrays}
\usage{
\method{cbind}{mlx}(..., deparse.level = 1)
}
\arguments{
\item{...}{Objects to bind. mlx arrays are kept in MLX; other inputs are
coerced via \code{as_mlx()}.}

\item{deparse.level}{Compatibility argument accepted for S3 dispatch; ignored.}
}
\value{
An mlx array stacked along the second axis.
}
\description{
Column-bind mlx arrays
}
\details{
Unlike base R's \code{cbind()}, this function supports arrays with more
than 2 dimensions and preserves all dimensions except the second (which is
summed across inputs). Base R's \code{cbind()} flattens higher-dimensional arrays
to matrices before binding.
}
\examples{
x <- as_mlx(matrix(1:4, 2, 2))
y <- as_mlx(matrix(5:8, 2, 2))
cbind(x, y)
}
\seealso{
\href{https://ml-explore.github.io/mlx/build/html/python/array.html#mlx.core.concatenate}{mlx.core.concatenate}
}
